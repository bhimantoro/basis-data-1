1
pada syntax ini akan menampilkan data dari tabel dosen dan matakuliah "SELECT * FROM dosen d, matakuliah mk" yang memiliki kode dosen yang sama "WHERE d.kode_dos=mk.kode_dos", dimana dosen tersebut mengajar matakuliah dengan sks yang paling kecil dari sks pada matakuliah yang lain "mk.sks < ANY (SELECT sks FROM matakuliah)".
2
pada query ini, akan ditampilkan data mahasiswa "SELECT m.nim, ...." yang mengambil mata kuliah dengan kode_mk yaitu "pti777", dimana pencocokan data menggunakan operator komparasi "IN" pada field nim pada tabel mahasiswa dan kd_mk pada tabel matakuliah "WHERE (m.nim, mk.kd_mk)" dengan kd_mk pada tabel ambil_mk yang memiliki record kd_mk yaitu "pti777" pada subquery (WHERE a.kd_mk="PTI777")
3
pada query ini, ditampilkan data mahasiswa "SELECT * FROM mahasiswa m" yang memiliki tempat tinggal satu wilayah dengan kajur TE, pencocokan menggunakan scalar subquery dimana, alamat dosen mencocokkan menggunakan kode_dos pada tabel dosen dan jurusan "WHERE d.kode_dos=j.kode_dos" dicocokkan dengan dosen memiliki kode jurusan TE "j.kode_jur="TE" dengan alamat pada data mahasiswa "WHERE m.alamat = ".
4
pada query ini, akan ditampilkan data mahasiswa "SELECT * FROM mahasiswa m" yang tidak diajar oleh dosen yang merupakan kajur dari teknik elektro. pencocokan dimulai dari mencocokkan data mahasiswa dimana nim mahasiswa tidak terdapat "WHERE m.nim NOT IN" pada field nim dari tabel ambil_mk "(SELECT a.nim FROM ambil_mk a", lalu kode matakuliah pada tabel ambil_mk dicocokkan dengan kode matakuliah dari tabel matakuliah "WHERE a.kd_mk IN (SELECT mk.kd_mk FROM matakuliah mk", dilanjutkan dengan pencocokkan kode dosen pada tabel matakuliah dengan kode dosen pada tabel dosen "WHERE mk.kode_dos IN (SELECT d.kode_dos FROM dosen d", dan kode dosen pada tabel dosen dicocokkan dengan kode dosen pada tabel jurusan "WHERE d.kode_dos IN (SELECT j.kode_dos FROM jurusan j", dimana dosen tersebut memiliki kode jurusan yakni TE "WHERE j.kode_jur="TE"